# Erzeugte Verzeichnisse
dist/
coverage/
.angular/
node_modules/

# Von Supabase erzeugt bzw. temporaer
supabase/.temp/
supabase/.branches/
src/app/core/models/supabase.types.ts

# Sperrdateien
package-lock.json

# Abbilder der Serverkonfiguration - sollen zeichengenau dem entsprechen,
# was auf dem Server liegt. Umformatieren wuerde sie auseinanderlaufen lassen.
deploy/Caddyfile
deploy/backup.sh
deploy/*.yml

# Abbilder fremder Antworten. Sie muessen zeichengenau so bleiben, wie die
# Gegenstelle sie liefert. Beim Vinted-Fixture steckt der Kategoriebaum in
# einem JS-String-Literal mit escapten Anfuehrungszeichen (\"); ein Formatierer
# schreibt sie in einfache Anfuehrungszeichen um und zerstoert damit genau die
# Stelle, an der ein naiver Parser scheitern soll. Das Fixture pruefte danach
# nichts mehr, sondern taeuschte den Parser.
services/sniper/test/fixtures/
